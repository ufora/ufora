//TODO FEATURE anybody: break out each python wrapper into a separate .py.cpp file
//
//Ideally we'd have an auto-registration model like the boost unit-test model
//so that we wouldn't need to register the wrapper in module.hpp